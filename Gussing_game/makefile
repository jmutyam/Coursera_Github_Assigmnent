README.md:
          touch README.md
          echo "# Gussing game" > README.md
          echo "Make was run on $(date)" >> README.md
          echo "gussingame.sh contains $(wc -1 < gussinggame.sh) lines of code." >> README.md
          
          
          
clean:
         rm README.md
